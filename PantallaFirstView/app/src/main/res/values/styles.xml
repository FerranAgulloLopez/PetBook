<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="text_error" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FF0101</item>
        <item name="android:textColorHint">#FF0101</item>
    </style>

    <style name="text_success" parent="@android:style/TextAppearance">
        <item name="android:textColor">#40AC48</item>
        <item name="android:textColorHint">#40AC48</item>
    </style>

</resources>
